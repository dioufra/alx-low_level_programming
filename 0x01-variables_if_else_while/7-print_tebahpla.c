#include <stdio.h>

/**
 * main - Print alphabet
 * Description: Prints the alphabet in lowercase reverse,
 *followed by a new line
 * Return: Always 0 (Success)
 */

int main(void)
{
	char letter = 'a';

	while (letter <= 'a')
	{
		putchar(letter);
		l--;
	}
	putchar('\n');
	return (0);
}
