#include "main.h"

/**
 * swap_int - swaps the value of two integer
 * @a: pointer to the address of the first integer
 * @b: pointer to the address of the second integer
 */
void swap_int(int *a, int *b)
{
	*a, *b = *b, *a;
}
